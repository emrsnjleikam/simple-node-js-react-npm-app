pipeline {
    agent {
        docker {
            image 'node:latest' 
            args '-p 3000:3000' 
        }
    }
    stages {
        stage('Build') { 
            steps {
				sh 'ping -c 3 google.com'
                sh 'npm install' 
            }
        }
    }
}