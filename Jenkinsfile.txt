pipeline {
    agent {
        docker {
            image 'node:6-alpine' 
            args '-p 3000:3000 --add-host registry.npmjs.org:151.101.16.162' 
        }
    }
    stages {
        stage('Build') { 
            steps {
                sh 'npm install' 
            }
        }
    }
}