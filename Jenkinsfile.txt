pipeline {
    agent {
        docker {
            image 'node:latest' 
            args '-p 3000:3000' 
        }
    }
    stages {
        stage('Build') { 
            steps {
				sh 'npm config rm proxy'
				sh 'npm config rm https-proxy'
                sh 'npm install' 
            }
        }
    }
}